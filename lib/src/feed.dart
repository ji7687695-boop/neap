import 'package:flutter/material.dart';
import 'profile.dart';
import 'compose.dart';
class Post { final String user,text,distance,interest; const Post(this.user,this.text,this.distance,this.interest); }
const demoPosts=[Post('mii','この店よかった。また行きたい','近く','カフェ'),Post('sora','今日これ観てきた。普通に好き','近く','映画'),Post('kei','仕事終わり。ちょっとだけ飲む','同じエリア','酒')];
class FeedScreen extends StatefulWidget { const FeedScreen({super.key}); @override State<FeedScreen> createState()=>_FeedScreenState(); }
class _FeedScreenState extends State<FeedScreen>{int tab=0;@override Widget build(BuildContext context){final pages=[const Feed(),const Center(child:Text('通知')),const MyProfile()];return Scaffold(appBar:AppBar(title:const Text('neap',style:TextStyle(fontWeight:FontWeight.w800))),body:pages[tab],floatingActionButton:tab==0?FloatingActionButton(onPressed:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const ComposeScreen())),child:const Icon(Icons.add)):null,bottomNavigationBar:NavigationBar(selectedIndex:tab,onDestinationSelected:(i)=>setState(()=>tab=i),destinations:const[NavigationDestination(icon:Icon(Icons.home_outlined),selectedIcon:Icon(Icons.home),label:'Home'),NavigationDestination(icon:Icon(Icons.notifications_none),selectedIcon:Icon(Icons.notifications),label:'通知'),NavigationDestination(icon:Icon(Icons.person_outline),selectedIcon:Icon(Icons.person),label:'Me')]));}}
class Feed extends StatelessWidget{const Feed({super.key});@override Widget build(BuildContext context)=>ListView.separated(itemCount:demoPosts.length,separatorBuilder:(_,__)=>const Divider(height:1),itemBuilder:(_,i){final p=demoPosts[i];return Padding(padding:const EdgeInsets.all(16),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[const CircleAvatar(child:Icon(Icons.person)),const SizedBox(width:10),Text(p.user,style:const TextStyle(fontWeight:FontWeight.w700)),const Spacer(),Text(p.distance,style:const TextStyle(color:Colors.black54))]),const SizedBox(height:14),AspectRatio(aspectRatio:4/3,child:Container(decoration:BoxDecoration(color:Colors.black12,borderRadius:BorderRadius.circular(14)),child:const Icon(Icons.image_outlined,size:48))),const SizedBox(height:12),Text(p.text,style:const TextStyle(fontSize:16)),const SizedBox(height:7),Text('#${p.interest}',style:const TextStyle(color:Colors.black54)),const SizedBox(height:10),Row(children:[IconButton(onPressed:(){},icon:const Icon(Icons.favorite_border)),IconButton(onPressed:(){},icon:const Icon(Icons.chat_bubble_outline)),const Spacer(),TextButton.icon(onPressed:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>ProfileScreen(name:p.user))),icon:const Icon(Icons.open_in_new,size:18),label:const Text('Instagram'))])])) ;});}
